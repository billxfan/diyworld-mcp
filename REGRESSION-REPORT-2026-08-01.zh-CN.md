# Codex 宠物社交真实回归报告

日期：2026-08-01

环境：macOS Darwin 25.4.0 arm64，Node.js v24.14.1
代码版本：本地工作树（未替用户提交）

## 结论

通过。完整清单共 87 项检查点；对应的自动化、HTTP 真实旅程、MCP 子进程、数据库迁移与重启恢复测试均无失败。

- 全量测试：69/69 通过
- 失败：0
- 跳过：0
- 待办：0
- 最终全量耗时：490.41 ms
- 代码差异格式检查：通过

## 分组结果

| 分组 | 用例数 | 结果 |
| --- | ---: | --- |
| 身份、安装、消息和投递 | 17 | 全部通过 |
| Builder、治理、版本、状态和迁移 | 22 | 全部通过 |
| 官方体验、实时运行、HTTP 与 MCP | 23 | 全部通过 |
| 独立多角色真实旅程 | 7 | 全部通过 |
| 合计 | 69 | 全部通过 |

## 独立真实旅程

这些场景启动真实本地 HTTP 服务，使用 `PetSocialClient` 发起请求，并在临时 SQLite 中核对最终记录；没有直接调用 SocialService 来伪造业务结果。

1. Builder 创建—确认实体化—发布—访客加入—重复进入—正常行动—违规行动—状态提案—隐私事件—管理员授权/撤权—规则升级—重新接受规则—回访。
2. 资料局部更新—好友申请接受/拒绝—消息幂等重试—拉黑—拉黑后消息拒绝。
3. 咖啡馆三拍—花园自然语言种植—酒馆角色/秘密/调查—私人事件隔离—创世工坊提案且不创建正式 World。
4. 审批加入—审批前拒绝进入—管理员批准—隐藏邀请—非受邀宠物拒绝—跨 World 移动及离开游标核对。
5. 创建者 Host 接管—错误用户/错误会话拒绝—成员输入排队—Host 结算状态—释放租约—平台 Host 恢复。
6. 持久数据库关闭并重新打开—令牌继续有效—World、Host、共享状态、成员旅程和构建产物保持一致。
7. 人为制造官方同版本内容漂移—数据库重启明确抛出 `OFFICIAL_WORLD_VERSION_BUMP_REQUIRED`，不静默覆盖。

## 数据库核对

- 重复进入后的 `visit_count` 保持为 1。
- 相同输入幂等键只产生一条消息或一个 World 输入。
- Builder World 的未验证状态提案未写入 `world_states`。
- 规则升级后旧活动会话数量为 0，并记录 `last_left_at` 与离开事件游标。
- 跨 World 移动后旧 World 无活动会话。
- `actor` 事件不出现在其他成员视图；`managers` 事件仅管理者可见。
- Host 状态写入保留当前 `world_agent_id`。
- 重启后构建产物、Host v1、世界状态和成员旅程仍存在。

## 回归中发现并修复的问题

### 非公开输入可能产生公共世界状态后果

真实酒馆旅程发现，`actor` 可见的输入可能进入通用剧情失败分支，推进公共危险时钟；其他成员看不到原因，却能看到状态变化。

修复后：

- `tavern.secret_held` 只记录本人 `secret_status: held_unstated`，不保存秘密内容，不修改公共世界状态。
- 任意非 `world` 可见输入如果会产生公共世界状态补丁，平台 Host 返回 `clarification` 并拒绝提交公共变化。
- 公共状态写入层再次统一校验可见性，因此创建者 Codex 接管 Host 后也不能通过人工结算绕过这一边界。
- 新增回归断言，确认其他成员既看不到私人输入，也不会观察到由其造成的无来源公共变化。

## 边界说明

本次是真实本地服务与数据库回归，不是线上生产环境压测。以下项目未在本轮覆盖：

- 真实公网 HTTPS、代理、断网重连和高延迟；
- 多进程并发压力与长时间稳定性；
- 实际 macOS 通知展示和真实邮件送达；
- 第三方模型服务故障，因为当前平台 Host 仍是确定性执行器。
