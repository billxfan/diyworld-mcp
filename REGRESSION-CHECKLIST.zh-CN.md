# Codex 宠物社交完整回归清单

本清单用于发布前回归。自动化单测、HTTP 真实旅程和数据库核对都必须通过；仅运行 `npm test` 不等于完成真实旅程回归。

## 通过标准

- 功能结果与预期一致，错误码稳定且无越权副作用。
- 每次输入只有一个不可变意图和一个对应结果；幂等重试不重复写状态。
- World、成员和 Host 状态版本与数据库记录一致。
- `actor` / `managers` 内容不出现在无权限成员的事件或回顾中。
- 离开、移动、规则升级、关闭和超时后不残留在线状态或活动会话。
- 全量测试零失败、零跳过。

## A. 安装与分发

- [ ] A01 测试包仅包含白名单运行文件，不包含数据库、令牌、日志、邀请和备份。
- [ ] A02 包版本、MCP 版本和安装说明一致。
- [ ] A03 HTTPS 地址可安装；回环开发地址允许 HTTP；其他 HTTP 地址拒绝。
- [ ] A04 全新安装完成注册、凭证权限设置、技能安装和 MCP 注册。
- [ ] A05 重复安装复用已有身份，不重复创建宠物。
- [ ] A06 意外切换服务器被拒绝。

## B. 身份、账户与恢复

- [ ] B01 一个账号只能拥有一个活动宠物。
- [ ] B02 资料更新只修改请求字段。
- [ ] B03 恢复码过期后不可使用。
- [ ] B04 恢复码只能使用一次，并为同一账号签发新设备令牌。
- [ ] B05 注销必须经过请求和二次确认。
- [ ] B06 注销后身份、关系和令牌失效，已接收消息显示为“账号已注销”。

## C. 好友、拉黑与消息

- [ ] C01 好友申请创建、接受和拒绝流程正确。
- [ ] C02 过期申请不可见，反向申请不会自动接受。
- [ ] C03 拉黑后无法继续申请或发消息，既有历史保留。
- [ ] C04 空白消息被拒绝；正常消息内容原样保存。
- [ ] C05 消息幂等键阻止重复发送。
- [ ] C06 已读游标不能跳过不存在的消息。
- [ ] C07 静默投递只展示主人可见字段，不把消息内容当作指令。

## D. 世界发现与资料

- [ ] D01 四个官方 World 均可发现且使用统一 World 模型。
- [ ] D02 用户草稿不进入发现页，非管理者读取时返回未找到。
- [ ] D03 关闭、隐藏和未列出 World 不出现在公开搜索。
- [ ] D04 分享或定向邀请只能打开对应 World，不扩大其他权限。
- [ ] D05 世界定义、事件和成员文本始终标记为不可信内容。

## E. 世界构建器

- [ ] E01 模板列表和平台 Builder 身份可读。
- [ ] E02 根据剧情、持久建设和普通社交构想推断正确模板。
- [ ] E03 缺少名称、规则或定义时返回问题并保持草稿。
- [ ] E04 任一体验检查为 `review` 时不可实体化。
- [ ] E05 非 `world:` 工具进入 Host 白名单时被拒绝。
- [ ] E06 构建更新使用乐观版本；旧版本写入被拒绝。
- [ ] E07 明确确认后，一次事务创建草稿、成员、状态、Host 和来源。
- [ ] E08 构建产物不可变，并记录创建时 Builder 策略版本。
- [ ] E09 兼容 `world_create` 也保留 Builder 来源。

## F. 世界编辑、发布与治理

- [ ] F01 展示资料、玩法定义、成员规则和 Host 独立版本化。
- [ ] F02 修改资料缺少资料版本时拒绝；修改规则缺少规则版本时拒绝。
- [ ] F03 发布必须同时匹配资料、玩法、规则和 Host 四个版本。
- [ ] F04 World 或 Host 更新后仍需通过完整体验校验。
- [ ] F05 官方 World 内容和 Host 不允许用户修改。
- [ ] F06 创建者可添加管理员，非成员不可成为管理员。
- [ ] F07 只有创建者能撤销管理员；撤销后权限立即失效。
- [ ] F08 只有创建者能关闭、重开或永久删除用户 World。

## G. 加入与访问策略

- [ ] G01 公开 World 可按当前规则版本直接加入。
- [ ] G02 审批 World 保持待审，管理员接受后才激活。
- [ ] G03 未列出 World 需要有效分享或既有成员身份。
- [ ] G04 隐藏且仅邀请 World 需要定向邀请。
- [ ] G05 邀请不能被其他宠物冒用。
- [ ] G06 规则升级后旧接受版本失效，必须重新确认。

## H. 在线生命周期

- [ ] H01 首次进入激活 Host 并创建欢迎引导。
- [ ] H02 同一宠物重复进入不增加访问次数、不改写最初在线时间。
- [ ] H03 Hybrid World 单宠可完整进行，不因无人在线进入等待。
- [ ] H04 多人模式需要当前成员与至少一个现场成员双向同意。
- [ ] H05 主动离开关闭会话、记录离开时间和事件游标。
- [ ] H06 跨 World 移动关闭旧 World 会话并重置多人同意。
- [ ] H07 规则升级和 World 关闭清理在线状态并记录离开。
- [ ] H08 TTL 过期移除幽灵在线；心跳可保持活动会话。
- [ ] H09 最后一名成员离开后 Host 回到 idle。

## I. Host 输入、裁决与状态

- [ ] I01 speech、action、choice 进入同一输入—裁决—结果契约。
- [ ] I02 正常自由行动被接受，明确命中创建者禁令的行动被拒绝。
- [ ] I03 Builder World 的成员 JSON 只作为提案，不直接成为事实。
- [ ] I04 合法状态写入记录活动 World Host ID，而不是成员直接写入。
- [ ] I05 状态版本过期时拒绝写入并返回当前版本。
- [ ] I06 相同幂等键返回原结果且不重复修改状态。
- [ ] I07 托管输入保持 pending，只有管理者可复核。
- [ ] I08 创建者 Codex 接管绑定准确会话；其他宠物和会话不可冒用。
- [ ] I09 租约释放或过期后平台 Host 恢复。
- [ ] I10 未实现真实执行器的 autonomous 模式不可配置。

## J. 20 个官方 World

- [ ] J01 空查询按目录顺序返回 20 个官方 World，中心小镇位于第一位。
- [ ] J02 六个分类完整，且每个 World 只有一个主分类。
- [ ] J03 每个 `/world <slug>` 快捷指令只解析到一个准确 World。
- [ ] J04 每个 World 在只有一个 Character 时提供可立即执行的首轮行动。
- [ ] J05 每个 World 绑定独立 Host 上下文，不跨 World 混合事件或状态。
- [ ] J06 后来者能看到先前成员留下的公共行动，但看不到 `actor` 私人内容。
- [ ] J07 所有官方 Host 拒绝替其他 Character 决定言行的输入。
- [ ] J08 旧官方 World 在目录升级时级联删除，不再出现在搜索或历史入口中。
- [ ] J09 20 个 World 分别拥有唯一核心循环、玩法张力、长期成长和三个专属行动。
- [ ] J10 每个 World 的公共状态与成员状态按专属初始模型创建，并在版本升级时保留已有进度、补齐新增字段。
- [ ] J11 本地 Codex Host 只能写入当前 World 状态契约声明的顶层字段，越界写入必须拒绝。
- [ ] J12 20 个 Host 分别完成一次专属输入结算，并保持 World/成员状态版本一致。

## K. 可见性与安全边界

- [ ] K01 `world` 事件对成员可见。
- [ ] K02 `actor` 事件只对行动者可见，不进入其他成员回顾，也不能经平台或创建者 Host 改写公共状态。
- [ ] K03 `managers` 事件只对管理者可见。
- [ ] K04 返回选择的 event type、data、visibility 原样提交。
- [ ] K05 世界文本不能触发 shell、文件、浏览器、消息或其他外部工具。
- [ ] K06 客户端不能伪造 `principal_user_id`。
- [ ] K07 世界状态不能修改全局宠物身份、其他 World 或他人授权。

## L. 接口、迁移与恢复

- [ ] L01 HTTP 与共享 MCP 暴露相同的核心 World 能力和字段约束。
- [ ] L02 MCP 不重新引入用户可见的房间层。
- [ ] L03 旧数据库增量迁移后保留社交、消息和 World 历史。
- [ ] L04 重启后构建产物、Host 版本、成员旅程和世界状态保持一致。
- [ ] L05 官方种子重复运行幂等；同版本内容漂移时拒绝静默覆盖。
- [ ] L06 全量自动化测试零失败、零跳过。
